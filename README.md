# pangolin-update.sh

**Версия:** 0.3.4 · **Дата:** 19.06.2026

---

## Содержание

- [Описание](#описание)
- [Компоненты стека](#компоненты-стека)
- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Использование](#использование)
- [Редакции: Community / Enterprise](#редакции-community--enterprise)
- [Логика работы](#логика-работы)
- [Health check](#health-check)
- [Автоматический откат](#автоматический-откат)
- [Логирование](#логирование)
- [Резервные копии](#резервные-копии)
- [Автоматизация через cron](#автоматизация-через-cron)
- [Коды выхода](#коды-выхода)
- [Структура файлов](#структура-файлов)

## Не входит в скрипт
- [Установка и Обновление агента newt](#установка-и-обновление-newt)

---

## Описание

Скрипт проверяет актуальность версий всех компонентов Pangolin-стека, сравнивает их с установленными и при наличии обновлений выполняет полный цикл обновления. После запуска новых контейнеров производится многоступенчатая проверка работоспособности. Если стек не прошёл проверку — скрипт автоматически откатывает конфигурацию и поднимает предыдущие версии из локального кэша образов.

**Порядок выполнения при наличии обновлений:**

1. Создание резервных копий конфигурационных файлов
2. Остановка Docker-стека
3. Обновление версий в `docker-compose.yml` и `traefik_config.yml`
4. Скачивание новых Docker-образов
5. Запуск стека на новых версиях
6. **Health check** — проверка состояния контейнеров (с повторными попытками)
7. При успехе — удаление устаревших образов
8. При сбое — **автоматический откат** к предыдущим версиям

> Удаление старых образов (`docker image prune`) происходит **только после успешного** прохождения health check, что гарантирует возможность отката.

> Режим `-info` (см. [Использование](#использование)) останавливается сразу после таблицы сравнения версий и не выполняет ни одного из шагов выше — удобно для проверки «что обновится» без изменений.

---

## Компоненты стека

| Компонент | Источник версий | Описание |
|-----------|----------------|----------|
| **Pangolin** | GitHub Releases (`fosrl/pangolin`) | Основное приложение |
| **Gerbil** | GitHub Releases (`fosrl/gerbil`) | Вспомогательный сервис |
| **Traefik** | GitHub Releases (`traefik/traefik`) | Reverse-proxy |
| **Badger** | GitHub Releases (`fosrl/badger`) | Плагин Traefik |

> **Версия Traefik.** Берётся с GitHub Releases (а не с Docker Hub). Сначала запрашивается `/releases/latest`; если он указывает на чужую мажорную ветку (например, в день выхода патча ветки v2.11.x) или возвращает пустой/битый ответ, скрипт добирает последнюю стабильную версию **строго в пределах текущей мажорной ветки**. Мажор определяется автоматически из установленной версии, поэтому стек никогда не «перепрыгнет» на другой major (v2 ↔ v3 ↔ будущий v4), где возможны несовместимые изменения. Срабатывание запасного пути фиксируется в логе уровнем `WARN`.

---

## Требования

- **OS:** Linux (Bash 4.0+)
- **Права:** root (или пользователь с доступом к Docker)
- **Зависимости:**

| Утилита | Назначение |
|---------|-----------|
| `curl` | Запросы к GitHub API (версии всех компонентов) |
| `jq` | Парсинг JSON-ответов |
| `docker` | Управление контейнерами (`compose`, `inspect`, `logs`) |
| `sed` | Замена версий в конфигурационных файлах |
| `awk` | Извлечение версии Badger из YAML |

Установка зависимостей на Debian/Ubuntu:

```bash
apt install curl jq docker.io
```

---

## Установка

```bash
# Скачать скрипт
curl -o /usr/local/bin/pangolin-update.sh \
     https://your-host/pangolin-update.sh

# Сделать исполняемым
chmod +x /usr/local/bin/pangolin-update.sh

# Создать файл лога
touch /var/log/pangolin-update.log
```

---

## Конфигурация

Все настройки сосредоточены в блоке **«Конфигурация»** в начале скрипта:

```bash
# Пути
PANGOLIN_DIR="/root/pangolin"
COMPOSE_FILE="${PANGOLIN_DIR}/docker-compose.yml"
TRAEFIK_CONFIG="${PANGOLIN_DIR}/config/traefik/traefik_config.yml"
LOG_FILE="/var/log/pangolin-update.log"
LOCK_FILE="/tmp/pangolin-update.lock"
LOG_MAX_LINES=1000       # ротация лога при превышении этого числа строк

# Параметры health check
HEALTH_INITIAL_WAIT=20   # пауза после запуска перед первой проверкой (сек)
HEALTH_INTERVAL=10       # интервал между повторными проверками (сек)
HEALTH_RETRIES=6         # максимальное количество попыток
RESTART_THRESHOLD=3      # порог перезапусков контейнера, считается аномалией
```

Суммарное время ожидания health check: `HEALTH_INITIAL_WAIT + HEALTH_INTERVAL × HEALTH_RETRIES` = **80 секунд** при настройках по умолчанию.

---

## Использование

### Запуск вручную

```bash
bash /usr/local/bin/pangolin-update.sh
```

### Аргументы командной строки

| Аргумент | Действие |
|----------|----------|
| *(без аргументов)* | Полный цикл: проверка версий и, при наличии обновлений, обновление стека с health-check и автоматическим откатом. |
| `-info`, `--info` | **Только** проверка и вывод таблицы «Сравнение версий». Никаких действий не выполняется (dry-run): без резервных копий, остановки стека, изменения конфигов, скачивания и удаления образов. |
| `-enterprise`, `--ee` | Использовать/переключиться на **Enterprise Edition** (образ `fosrl/pangolin:ee-*`). См. [Редакции](#редакции-community--enterprise). |
| `-community`, `--ce` | Использовать/вернуться на **Community Edition** (образ `fosrl/pangolin:*`). |
| `-h`, `--help` | Показать справку и выйти. |

> Без флага редакции текущая редакция Pangolin определяется автоматически по тегу образа в `docker-compose.yml` и сохраняется. Поэтому запуск из cron без флага не переключит редакцию случайно.

> В режиме `-info` lock-файл **не захватывается** — команду можно безопасно запускать параллельно с идущим обновлением (она не блокируется и не мешает ему). Учтите, что `-info` всё же пишет ход проверки в `LOG_FILE`, поэтому запускать его следует с правами на запись в лог (обычно от root). Справка `-h` работает без каких-либо прав.

### Пример вывода — режим `-info`

```
────────────────────────────────────────────────────────────
[2026-06-19 03:00:01] [INFO] Проверка версий Pangolin-стека (режим -info, изменения не вносятся)
[2026-06-19 03:00:01] [INFO] Чтение текущих версий из конфигурации...
[2026-06-19 03:00:01] [INFO] Запрос актуальных версий...
[2026-06-19 03:00:03] [INFO] Сравнение версий:
  Pangolin     v1.2.0         -> v1.3.0
  Gerbil       v1.0.5         (без изменений)
  Traefik      v3.2.0         -> v3.7.4
  Badger       v1.1.0         (без изменений)
[2026-06-19 03:00:03] [INFO] Доступно обновление. Режим -info: действия не выполняются.
```

> Код выхода в режиме `-info` всегда `0` (если все компоненты актуальны — выводится «обновление не требуется»).

### Пример вывода — обновление прошло успешно

```
────────────────────────────────────────────────────────────
[2026-04-14 03:00:01] [INFO] Запуск проверки обновлений Pangolin-стека
[2026-04-14 03:00:03] [INFO] Сравнение версий:
  Pangolin     v1.2.0         -> v1.3.0
  Gerbil       v1.0.5         (без изменений)
  Traefik      v3.2.0         -> v3.3.1
  Badger       v1.1.0         (без изменений)
[2026-04-14 03:00:05] [OK]   Резервные копии созданы (суффикс: 14042026_030005)
[2026-04-14 03:00:17] [OK]   Стек остановлен
[2026-04-14 03:00:17] [OK]   Файлы конфигурации обновлены
[2026-04-14 03:01:04] [OK]   Образы скачаны
[2026-04-14 03:01:04] [OK]   Стек запущен, начинаем проверку работоспособности...
[2026-04-14 03:01:24] [INFO] Проверка состояния стека (попытка 1/6)...
  ✓ pangolin-pangolin-1      состояние=running     здоровье=none         перезапусков=0
  ✓ pangolin-gerbil-1        состояние=running     здоровье=none         перезапусков=0
  ✓ pangolin-traefik-1       состояние=running     здоровье=none         перезапусков=0
[2026-04-14 03:01:24] [OK]   Health check пройден: все контейнеры работают корректно
[2026-04-14 03:01:27] [OK]   Устаревшие образы удалены
[2026-04-14 03:01:27] [OK]   Обновление успешно завершено
────────────────────────────────────────────────────────────
```

### Пример вывода — сбой и откат

```
[2026-04-14 03:01:24] [INFO] Проверка состояния стека (попытка 1/6)...
  ✗ pangolin-pangolin-1      состояние=restarting  здоровье=none         перезапусков=4
[2026-04-14 03:01:24] [WARN] Контейнер 'pangolin-pangolin-1': циклические перезапуски (4 раз)
...
[2026-04-14 03:03:04] [ERROR] Health check провален после 6 попыток
[2026-04-14 03:03:04] [ERROR] Логи 'pangolin-pangolin-1' записаны в /var/log/pangolin-update.log
[2026-04-14 03:03:04] [ERROR] Новые версии не прошли проверку работоспособности — запускаем откат
[2026-04-14 03:03:04] [WARN]  ┌─────────────────────────────────────────────┐
[2026-04-14 03:03:04] [WARN]  │      ЗАПУСК ОТКАТА К ПРЕДЫДУЩЕЙ ВЕРСИИ      │
[2026-04-14 03:03:04] [WARN]  └─────────────────────────────────────────────┘
[2026-04-14 03:03:16] [OK]   [Откат] Контейнеры остановлены
[2026-04-14 03:03:16] [OK]   [Откат] docker-compose.yml восстановлен
[2026-04-14 03:03:16] [OK]   [Откат] traefik_config.yml восстановлен
[2026-04-14 03:03:17] [OK]   [Откат] Команда запуска выполнена
[2026-04-14 03:03:37] [INFO] [Откат] Проверка работоспособности восстановленного стека...
  ✓ pangolin-pangolin-1      состояние=running     здоровье=none         перезапусков=0
  ✓ pangolin-gerbil-1        состояние=running     здоровье=none         перезапусков=0
  ✓ pangolin-traefik-1       состояние=running     здоровье=none         перезапусков=0
[2026-04-14 03:03:37] [OK]   Откат выполнен успешно. Стек работает на старых версиях
```

### Пример вывода — обновления не требуются

```
[2026-04-14 03:00:05] [INFO] Все компоненты актуальны, обновление не требуется.
```

---

## Редакции: Community / Enterprise

Сервис `pangolin` может работать в двух редакциях из одного и того же релиза (одинаковая схема БД, переключение — это смена тега образа):

| Редакция | Тег образа `pangolin` | Лицензия |
|----------|----------------------|----------|
| **Community (CE)** | `fosrl/pangolin:vX.Y.Z` | не требуется |
| **Enterprise (EE)** | `fosrl/pangolin:ee-X.Y.Z` | ключ активируется вручную в `/admin/license` |

Подробности о лицензировании и функциях EE — в [официальной документации](https://docs.pangolin.net/self-host/enterprise-edition). Редакция влияет только на образ `pangolin`; `gerbil`, `traefik` и `badger` от неё не зависят.

### Как скрипт выбирает редакцию

- **Без флага** редакция определяется автоматически по текущему тегу образа: `ee-*` → Enterprise, иначе Community. Текущая редакция **сохраняется** — это безопасно для cron (случайного переключения не произойдёт).
- **`-enterprise`** (он же `--ee`) переключает на EE: при ближайшем обновлении тег станет `ee-X.Y.Z`.
- **`-community`** (он же `--ce`) возвращает на CE: тег станет `vX.Y.Z`.

Версия EE-образа выводится из того же релиза `fosrl/pangolin` на GitHub: тег `vX.Y.Z` преобразуется в `ee-X.Y.Z`.

### Переключение редакции

```bash
# Перейти на Enterprise
pangolin-update.sh -enterprise

# Вернуться на Community
pangolin-update.sh -community
```

При смене редакции скрипт:

1. выводит предупреждение `WARN` о смене и рекомендует сделать резервную копию БД (страховка, хотя схема общая);
2. меняет тег образа, скачивает его, перезапускает стек и проходит обычный health-check (при сбое — автоматический откат к прежней редакции);
3. после успешного перехода на EE напоминает активировать лицензионный ключ в панели Server Admin (`/admin/license`) — без ключа EE-функции остаются выключенными.

> **Активация лицензии** выполняется только вручную через веб-интерфейс — скрипт её не автоматизирует. Получить бесплатный ключ для личного использования можно на [app.pangolin.net](https://app.pangolin.net).

> **Примечание.** Скрипт предполагает, что для каждого релиза существует соответствующий тег `ee-X.Y.Z`. Если нужного EE-тега ещё нет, `docker compose pull` не сможет его скачать — в этом случае скрипт автоматически откатится на прежнюю версию, а тег можно указать вручную (`ee-latest` или конкретный) в `docker-compose.yml`.

---

## Логика работы

```
Старт
  │
  ├─► Разбор аргументов        (-info → dry-run; -h → справка)
  ├─► Ротация лога            (если > LOG_MAX_LINES строк; пропускается в -info)
  ├─► Проверка зависимостей   (curl, jq, docker, sed, awk)
  ├─► Захват lock-файла       (защита от параллельного запуска; пропускается в -info)
  ├─► Чтение текущих версий   (из docker-compose.yml и traefik_config.yml)
  ├─► Определение редакции    (CE/EE по тегу образа; флаг важнее автоопределения)
  ├─► Запрос актуальных версий (GitHub API; Traefik закреплён на текущем major)
  ├─► Вывод таблицы сравнения
  │
  ├─[режим -info]──────────► Выход (код 0), без изменений
  │
  ├─[Все версии совпадают]──► Выход (код 0)
  │
  └─[Есть обновления]
        │
        ├─► Создание резервных копий конфигов  (.bak.DDMMYYYY_HHMMSS)
        ├─► docker compose down
        ├─► sed: замена версий в конфигурационных файлах
        ├─► docker compose pull ──[ошибка скачивания]──► Откат
        ├─► docker compose up -d
        │
        └─► Health check (с повторами)
              │
              ├─[Успех]──► docker image prune ──► Выход (код 0)
              │
              └─[Сбой]
                    │
                    ├─► dump_failed_logs  (логи упавших контейнеров → LOG_FILE)
                    │
                    └─► Откат
                          ├─► docker compose down
                          ├─► Восстановление конфигов из резервных копий
                          ├─► docker compose up -d  (старые образы в кэше)
                          ├─► Проверка состояния после отката
                          │
                          ├─[Откат успешен]──► Выход (код 1)
                          └─[Откат провален]──► Выход (код 2)  ⚠ ручное вмешательство
```

---

## Health check

После запуска стека скрипт проверяет каждый контейнер через `docker inspect` по трём критериям:

| Критерий | Условие прохождения |
|----------|-------------------|
| `State.Status` | Значение равно `running` |
| `RestartCount` | Меньше `RESTART_THRESHOLD` (по умолчанию 3) |
| `State.Health.Status` | `healthy` или `none` (healthcheck не определён) |

Если хотя бы один контейнер не удовлетворяет критериям — попытка считается неудачной. Скрипт повторяет проверку через `HEALTH_INTERVAL` секунд, всего до `HEALTH_RETRIES` раз.

### Временна́я шкала health check (значения по умолчанию)

```
Запуск контейнеров
       │
    +20с  (HEALTH_INITIAL_WAIT)
       │
   Попытка 1 ──[OK]──► Готово
       │
    +10с  (HEALTH_INTERVAL)
       │
   Попытка 2 ──[OK]──► Готово
       │
    ...до 6 попыток (HEALTH_RETRIES)
       │
   Провал ──► Откат
```

Максимальное суммарное время ожидания: **80 секунд**.

### Диагностика при сбое

При провале health check скрипт автоматически собирает последние 50 строк логов каждого проблемного контейнера и записывает их в `LOG_FILE`. Это позволяет разобраться в причине сбоя без дополнительных команд.

---

## Автоматический откат

Откат запускается автоматически, если health check провален после всех попыток.

### Механизм отката

Старые образы Docker **не удаляются** до успешного прохождения health check, поэтому они гарантированно доступны в локальном кэше для немедленного перезапуска без скачивания.

**Шаги отката:**

1. `docker compose down` — остановка контейнеров с новыми версиями
2. Восстановление `docker-compose.yml` из резервной копии `.bak.ДАТА`
3. Восстановление `traefik_config.yml` из резервной копии `.bak.ДАТА`
4. `docker compose up -d` — запуск на старых образах из кэша
5. Проверка состояния контейнеров после отката

### Ручное восстановление

Если автоматический откат по какой-то причине тоже завершился неудачно (код выхода `2`), восстановить стек можно вручную:

```bash
cd /root/pangolin

# Найти резервные копии
ls -lt docker-compose.yml.bak.*

# Восстановить конфиги (заменить суффикс на актуальный)
cp docker-compose.yml.bak.14042026_030005 docker-compose.yml
cp config/traefik/traefik_config.yml.bak.14042026_030005 config/traefik/traefik_config.yml

# Поднять стек
docker compose up -d

# Убедиться, что всё запустилось
docker compose ps
```

---

## Логирование

Лог записывается в `/var/log/pangolin-update.log`.

### Формат записи

```
[YYYY-MM-DD HH:MM:SS] [LEVEL] Сообщение
```

### Уровни

| Уровень | Цвет в терминале | Описание |
|---------|-----------------|----------|
| `INFO` | Голубой | Информационные сообщения о ходе работы |
| `OK` | Зелёный | Успешное завершение шага |
| `WARN` | Жёлтый | Предупреждения и сообщения об откате |
| `ERROR` | Красный (stderr) | Критические ошибки |

> Цвета выводятся только в интерактивном терминале (TTY). При запуске через cron лог пишется без escape-последовательностей.

### Ротация лога

При превышении `LOG_MAX_LINES` (по умолчанию 1000 строк) текущий лог переименовывается:

```
/var/log/pangolin-update.log  →  /var/log/pangolin-update.log.YYYYMMDD
```

Старые файлы не удаляются автоматически — при необходимости настройте `logrotate`.

### Просмотр лога

```bash
# Последние 50 строк
tail -50 /var/log/pangolin-update.log

# Только ошибки
grep '\[ERROR\]' /var/log/pangolin-update.log

# Только откаты
grep 'ОТКАТ\|Откат' /var/log/pangolin-update.log

# История успешных обновлений
grep 'Обновление успешно завершено' /var/log/pangolin-update.log

# Провалы health check
grep 'Health check провален' /var/log/pangolin-update.log

# Случаи, когда сработал запасной путь определения версии Traefik
grep 'Traefik: latest вернул' /var/log/pangolin-update.log
```

---

## Резервные копии

Перед каждым обновлением создаются копии конфигурационных файлов:

```
docker-compose.yml                →  docker-compose.yml.bak.14042026_030005
config/traefik/traefik_config.yml →  config/traefik/traefik_config.yml.bak.14042026_030005
```

Суффикс включает дату и время (`DDMMYYYY_HHMMSS`), что исключает коллизии при нескольких запусках в день.

---

## Автоматизация через cron

Для ежедневной проверки обновлений в 3:00 ночи:

```bash
crontab -e
```

```cron
0 3 * * * /usr/local/bin/pangolin-update.sh
```

Для еженедельной проверки (в воскресенье в 4:00):

```cron
0 4 * * 0 /usr/local/bin/pangolin-update.sh
```

> При запуске через cron цвета в логе отключаются автоматически (нет TTY). Все ключевые сообщения пишутся в `LOG_FILE`.

> Флаг редакции в cron-строке указывать **не нужно**: скрипт сам определяет текущую редакцию (CE/EE) по тегу образа и сохраняет её. Одна и та же cron-строка корректно обслуживает и Community-, и Enterprise-стек.

---

## Коды выхода

| Код | Ситуация | Действие |
|-----|----------|----------|
| `0` | Успех: обновление выполнено, не требуется, либо запуск в режиме `-info` | — |
| `1` | Health check провален, откат выполнен успешно | Стек работает на предыдущих версиях. Проверьте лог для диагностики. |
| `2` | Health check провален, откат тоже провален | **Требуется ручное вмешательство.** Используйте резервные копии `*.bak.ДАТА`. |

---

## Структура файлов

```
/root/pangolin/
├── docker-compose.yml                          # Конфиг стека (обновляется скриптом)
├── docker-compose.yml.bak.DDMMYYYY_HHMMSS      # Резервная копия
└── config/
    └── traefik/
        ├── traefik_config.yml                  # Конфиг Traefik/Badger (обновляется скриптом)
        └── traefik_config.yml.bak.DDMMYYYY_HHMMSS  # Резервная копия

/var/log/
├── pangolin-update.log                         # Текущий лог
└── pangolin-update.log.YYYYMMDD                # Архивный лог (после ротации)

/tmp/
└── pangolin-update.lock                        # Lock-файл (удаляется при завершении)

/usr/local/bin/
└── pangolin-update.sh                          # Скрипт
```



## Установка и Обновление newt

Создание файла службы:

```bash
sudo nano /etc/systemd/system/newt.service
```

Содержимое файла:

```bash
[Unit]
Description=Newt Agent
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/newt --id 9***on --secret py5***qj66 --endpoint https://domain.tld
Restart=always
RestartSec=5
User=root
WorkingDirectory=/usr/local/bin

# Optional hardening
LimitNOFILE=65536
NoNewPrivileges=true

[Install]
WantedBy=multi-user.target

systemctl daemon-reload
```
Обновите конфигурации служб

```bash
systemctl daemon-reload
```

Для обновления сначала необходимо остановить службу: `systemctl stop newt.service`

Загрузите новый бинарный файл newt^

```bash
NEWT_V=$(curl -s https://api.github.com/repos/fosrl/newt/releases/latest | grep '"tag_name"' | cut -d '"' -f 4) \
  && [ -n "$NEWT_V" ] \
  && wget "https://github.com/fosrl/newt/releases/download/$NEWT_V/newt_linux_amd64" -O /usr/local/bin/newt \
  && chmod +x /usr/local/bin/newt
```

Запустите службу: `systemctl start newt.service`
